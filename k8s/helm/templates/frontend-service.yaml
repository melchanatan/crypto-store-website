apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.frontend.namespace }}
spec:
  type: {{ .Values.ingress.serviceType }}
  ports:
  - port: 80
    targetPort: {{ .Values.frontend.port }}
    protocol: TCP
  selector:
    app: {{ .Release.Name }}